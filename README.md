new - Read a file or text from stdin, then prompt user with matchup choices, updating a local json file with item rankings
load - Load a json file with a set of ranked items, and prompt the user
read - Load a json file and output ranking of the set of items
